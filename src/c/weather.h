#ifndef WEATHER_H
#define WEATHER_H

// Window for weather applet
static Window* weather_window;

static void weather_window_load(Window*);
static void weather_window_appear(Window*);
static void weather_window_disappear(Window*);
static void weather_window_unload(Window*);

#endif